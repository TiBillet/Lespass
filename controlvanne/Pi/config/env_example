# Généré par make claim. Les adresses ci-dessous sont MODIFIABLES à la main
# si le serveur change de domaine — puis : sudo systemctl restart tibeer
# / Generated by make claim. The addresses below CAN be edited by hand if
# the server domain changes — then: sudo systemctl restart tibeer

# Adresse du TENANT (le lieu) — utilisée par le Pi au quotidien (API + kiosk)
# / TENANT address (the venue) — used daily by the Pi (API + kiosk)
SERVER_URL=__SERVER_URL__
# Adresse RACINE du serveur — utilisée uniquement pour (re)appairer (make claim)
# / Server ROOT address — only used to (re)pair (make claim)
CLAIM_SERVER_URL=__CLAIM_SERVER_URL__
API_KEY=__API_KEY__
TIREUSE_UUID=__TIREUSE_UUID__
DEBUG=False
# Lecteur RFID
RFID_TYPE=__RFID_TYPE__
# GPIO
GPIO_VANNE=18
VALVE_ACTIVE_HIGH=True
GPIO_FLOW_SENSOR=23
FLOW_CALIBRATION_FACTOR=6.5
# Réseau
NETWORK_TIMEOUT=5.0
MAX_RETRIES=3
# Systemd
SYSTEMD_NOTIFY=True
# Git (mise à jour auto au démarrage + défauts du Makefile)
GIT_REPO=__GIT_REPO__
GIT_BRANCH=__GIT_BRANCH__
